import { TextInput } from '@gravity-ui/uikit';

export default function Search() {
  return (
    <TextInput
      placeholder="Поиск по имени"
      size="xl"
      style={{ width: '400px' }}
    />
  );
}
